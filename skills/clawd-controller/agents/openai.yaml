interface:
  display_name: "Clawd Controller"
  short_description: "Build scoped Claude Code task prompts"
  default_prompt: "Use $clawd-controller to turn my task into a structured Claude Code prompt with scope, requirements, and verification."
